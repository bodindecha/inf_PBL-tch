/* const PBL = (function(d) {
    const cv = {
        API_URL: "/t/PBL/v2/api/main-"
    };
    var sv = {
        started: false
    };
    var initialize = function() {
        if (!sv.started) {
            sv.started = true;
            // Process

        }
    };
    return {
        init: initialize
        
    };
}(document)); top.PBL = PBL; */